## API Report File for "@crazyorr/loan"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public
export function convertRate(rate: number, from: Frequency, to: Frequency): number;

// @public
export function convertTermUnit(num: number, from: TermUnit, to: TermUnit): number;

// @public
export const DAYS_IN_ONE_MONTH: number;

// @public
export const DAYS_IN_ONE_YEAR: number;

// @public
export class EvenPrincipalLoan extends InstallmentLoan {
    constructor(principalAmount: number, rate: number, rateType: Frequency, term: number, termUnit: TermUnit, repaymentFrequency: Frequency);
    // @override
    calculateInterest(): Repayment;
}

// @public
export class EvenTotalLoan extends InstallmentLoan {
    constructor(principalAmount: number, rate: number, rateType: Frequency, term: number, termUnit: TermUnit, repaymentFrequency: Frequency);
    // @override
    calculateInterest(): Repayment;
}

// @public
export enum Frequency {
    DAILY = 0,
    MONTHLY = 1,
    YEARLY = 2
}

// @public
export class IllegalArgumentException extends Error {
    constructor(message?: string);
}

// @public
export abstract class InstallmentLoan extends Loan {
    protected constructor(principalAmount: number, rate: number, rateType: Frequency, term: number, termUnit: TermUnit, repaymentFrequency: Frequency);
    protected installmentInterestRate(): number;
    protected installmentNumber(): number;
}

// @public
export class InstallmentLoanRepayment extends Repayment {
    constructor(
    installmentRepayments: Repayment[], principal?: number, interest?: number);
    installmentRepayments: Repayment[];
}

// @public
export class InterestOnlyOnInstallmentLoan extends InstallmentLoan {
    constructor(principalAmount: number, rate: number, rateType: Frequency, term: number, termUnit: TermUnit, repaymentFrequency: Frequency);
    // @override
    calculateInterest(): Repayment;
}

// @public
export abstract class Loan {
    protected constructor(
    principalAmount: number,
    rate: number,
    rateType: Frequency,
    term: number,
    termUnit: TermUnit);
    abstract calculateInterest(): Repayment;
    protected principalAmount: number;
    protected rate: number;
    protected rateType: Frequency;
    protected term: number;
    protected termUnit: TermUnit;
    static validatePrincipalAmount(principalAmount: number): boolean;
    static validateRate(rate: number): boolean;
    static validateTerm(term: number): boolean;
}

// @public
export class LumpSumLoan extends Loan {
    constructor(principalAmount: number, rate: number, rateType: Frequency, term: number, termUnit: TermUnit);
    // @override
    calculateInterest(): Repayment;
}

// @public
export const MONTHS_IN_ONE_YEAR: number;

// @public
export class Repayment {
    constructor(
    principal?: number,
    interest?: number);
    interest: number;
    principal: number;
    sum(): number;
}

// @public
export enum TermUnit {
    DAY = 0,
    MONTH = 1,
    YEAR = 2
}

```
